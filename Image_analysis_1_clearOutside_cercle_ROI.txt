// Select all ROIs and combine them into a single selection
roiManager("Deselect"); // first deselect

roiManager("Select All"); // select all ROIs
roiManager("Combine"); // merges into a single composite selection

// Now Clear Outside applies to all the circles of a neckp
setBackgroundColor(250, 250, 250); // set white background
run("Clear Outside");